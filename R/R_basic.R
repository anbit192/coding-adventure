# assign variable 
a = 1 + 2
b <- 3
d = 4
A = 100

#print
print(a)
print(A)
a
A

#data types

##numeric
numeric_a = 1
numeric_b = 1.92
numeric_a
numeric_b

##logical 
logical_a = TRUE
logical_b = F

##character
character_a = "abc"
character_b = "xyz"

# vectors
v1 = c(19,22,33,46) # => numeric
v2 = c("an", "ban", "can", "dan") # => character
v3 = c(T, F , TRUE, TRUE) # => boolean
v4 = c(T, F, 1, 2, F, 0) # => numeric (T = 1, F = 0)
v5 = c(1, 2, "abc", TRUE) # => character

#list
my_list = list(v1, v2, v3, 1, 2, "abcxyz", list(1, 23, 3, 34))
my_list

#data_frames (A data frame is a list of variables of the same number of rows)
df = data.frame(name = v2, grade = v1) # v2 's length = v1 's length










